# Android PDF 导出验收

最新复核：2026-09-24；r5为本地未发布检查点。下方保留beta.6及中间失败记录。

本次目标：标准 PDF 包含纸面上的正文、公式、图表、笔迹、插图及原 PDF 背景；不能只包含 OCR 页面图。

## 当前验收结论（2026-09-24，r5）

本轮样例的模拟器与独立视觉复核通过。Mac ARM / API35 平板模拟器26项原生回归通过，其中7项PDF回归；88项应用JVM及3项探针JVM通过，Lint为0错误、9警告。

从应用缓存取得真实九页PDF `dist/todo-review/android-flattened-regression-r5.pdf`，经Poppler逐页独立渲染：标题与首段同页，长正文各页字号一致，页尾不再硬拆编号/句子，尾段最后两行一起换页。中文、公式、Mermaid、笔迹、图片及原PDF底图均保留。最终1–7页PNG与已完整目检的r4逐字节相同，8–9页另行目检。图片在 `dist/todo-review/pdf-r5-pages/`；日志为 `android-native-r5.log`，源清单为 `android-source-r5.json`。

仍未验收：真实平板/最低配置内存、大文件压力、长时间使用和逐字内容正确性。正常正文不再通过缩小掩盖实高超出，无法安全排入时会明确报错，不输出静默裁切的成功文件。

中间失败记录保留：r3虽24项原生通过，独立七页目检发现孤立标题、跨页字号变化和硬拆字；r4修复这些问题，但末尾仍有孤行，且全套一次材料选择测试遇到对话窗口同步失败（单独2项通过）。r5补通用段尾规则及测试窗口隔离后，全套26项通过。不得将中间版日志当作最终证据。

## 实现

- 从当前笔记冻结页几何、笔迹、图片位置和文字片段，并独立打开 PDF 背景。
- 后台逐页绘制基础图层；文字使用附着的离屏 WebView，等待离线字体、数学、图表及视觉状态回调。
- 导出保持各片段页码与位置。普通正文按源行、句末及可用高度分页，保持统一字号，标题跟随正文，段尾避免单独一行换页；仅不可拆公式、图表等允许整体等比适配。CSS高度先换算为设备像素，普通正文实高不符时明确失败。
- 整份 PDF 先写缓存临时文件，完成后再写用户选择的目标，目标流关闭后才报告成功。主线程文字渲染按页最多等待 30 秒，失败、超时和内存不足有错误提示。
- 导出支持取消，串行任务检查取消及提交状态，写出异常可靠传播；有总像素预算，页面关闭、任务拒绝和结束均清理导出视图及独立PDF资源。

## 回归重点

- 纯文字笔记：标题、中文、列表与正文出现在 PDF 中。
- 混合笔记：LaTeX 公式和 Mermaid 图表完成渲染，手写笔迹、插图的位置保持正确。
- 多页笔记：停在第一页直接导出，后续页的文字仍完整；跨页文字使用各自片段，不把整段内容重复画到每页。
- PDF 批注笔记：原 PDF 每页底图仍在，新增内容在正确页、正确位置。
- 视口独立：缩放、滚动、当前选区或编辑控件不进入导出文件。
- 失败处理：渲染失败、超时或写出失败不能显示“已导出标准 PDF”。生成阶段失败不应先截断用户选择的目标文件。
- OCR 保持原行为：整页数字化的页面图片仍刻意排除文字流，文字源另行提供，避免重复识别。

## 实际平板检查

1. 安装本轮 beta.6，导入 `dist/PadNote-排版体验.padnote.json`。
2. 在笔记中增加一段手写、一张图片，并让较长正文延续到下一页；再添加一个独立 LaTeX 文字对象。
3. 停在第一页，调整缩放后选择“导出 → 标准 PDF（页面已合并）”。完成后用独立 PDF 阅读器打开。
4. 逐页对照原笔记：标题、步骤编号、公式右端、图中文字和箭头、跨页正文、笔迹及插图都应存在；没有选框、操作手柄或工具栏。
5. 另导入一份至少两页的 PDF，在不同页添加文字与手写，然后重复导出，检查底图和新增内容。
6. 若公式或图表源码本身有误，检查导出与纸面上的错误提示一致。遇到资源加载失败、渲染超时或写出失败时，应明确提示失败，不能把缺内容的文件当作完成。

## beta.6历史验证记录

- Android beta.6 / code 45 构建通过，59 项 JVM 测试通过，0 失败/错误/跳过；Lint 0 错误，8 项既有 `SetTextI18n` 警告。
- `PdfExportTest` 两项原生回归编译通过，未在设备执行。
- 新增 `PdfExportTest` 两项原生回归：真实 PDF 回读检查图层/页码/跨页内容并对比 OCR 图；故障输出流检查写出异常确实向调用方传播。像素检查不是逐字 OCR，也不能证明推导内容正确。
- beta.6发布时没有可用Android真机或模拟器，原生测试当时未运行；2026-09-24已建立API35模拟器，后续证据以上方r3结论为准。

安装包为 `dist/PadNote-Android-0.18.0-beta.6-debug.apk`（1,552,841 bytes，code 45）。SHA-256：`91b3570f72ef18351bf9c76fdc9d0fd9710d8a30f78fea90e6237e315209a8a0`。包名和 v2 签名证书与旧 beta 版一致，远端/本机 APK 与 89 项源码输入均已核对；构建、Lint 和核验记录位于 `dist/pdf-export-review/beta6/`。本轮仅交付本地安装包。

以上路径相对于项目根目录。本轮不修改 iPad 导出，也不增加模型请求。大文件压力、移动设备实际耗时和导出文件大小仍需真机量测。

## 实现复核依据

- [Android WebView](https://developer.android.com/reference/android/webkit/WebView#postVisualStateCallback(long,%20android.webkit.WebView.VisualStateCallback))：DOM 更新不等于下一帧已可绘制；导出视图需满足附着与可见性条件，等待视觉状态回调。
- [Android PdfDocument](https://developer.android.com/reference/android/graphics/pdf/PdfDocument)：文档不是线程安全对象，逐页串行处理，已开始的页需要完成后再关闭文档。
