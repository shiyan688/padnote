# 安全问题

如果问题涉及读取未授权的笔记、泄露令牌、绕过配对或审批、写入任务目录之外的文件，请先私密报告。

在 [仓库 Security 页面](https://github.com/shiyan688/padnote/security/advisories)选择 **Report a vulnerability**。该入口需要仓库管理员启用；如果看不到入口，请只发一条不含漏洞细节的 Issue，请维护者提供私密联系渠道。不要把可利用步骤、私人笔记或凭据放进公开 Issue。

报告中可以包含受影响的版本、安装来源、系统、最小复现步骤、预期授权与实际行为。请使用自造样例，并移除 API 密钥、设备令牌、配对码、二维码、私有服务地址和个人文件。凭据已经泄露时，先在原服务撤销；删除公开内容不能撤销凭据。

目前处于预览阶段，不承诺旧 beta 的长期安全维护或固定响应时限。维护者会在修复发布时写明受影响版本、升级方式和已知限制。完整链路的支持证据见 [支持范围](docs/SUPPORT.md)。

## 边界

PadNote 连接助手运行在用户自己的电脑上。管理界面和设备 API 使用不同的回环端口；私网转发只能接到设备 API。配对设备有独立身份，可以逐个撤销。

私网、HTTPS 和配对解决的是连接与身份问题。Agent 在电脑上能执行什么，仍取决于其权限和审批配置。当前助手不提供操作系统级沙箱，也不保证不可信笔记中的指令绝不会影响 Agent。

默认测试使用本地假服务和示例数据，不需要真实模型或 Agent 密钥。贡献代码的 CI 不应取得签名、发布或服务凭据；不要为外部 PR 切换到带有这些凭据的运行环境。
