# L1 收敛

## 定义

函数列 \(f_n\) 在 \(L^1(\mathbb R)\) 中收敛到 \(f\)，是指

\[
\|f_n-f\|_1 \to 0.
\]

把范数展开：

\[
\|f_n-f\|_1
=\int_{\mathbb R}|f_n(x)-f(x)|\,dx
\to 0.
\]

## 直觉

不要只盯着某一个点。绝对值保证正负误差不会抵消，积分把整条函数图像之间的误差面积加起来。L1 收敛表示这块总面积趋近于零。

## 一个提醒

L1 收敛讨论的是整体误差；它不直接声称每个点都收敛。
